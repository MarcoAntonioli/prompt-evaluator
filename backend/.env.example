# OCI Configuration for LiteLLM
# These credentials are used by LiteLLM to authenticate with OCI Generative AI

# OCI User OCID (required)
OCI_USER_OCID=your_user_ocid_here

# OCI API Key Fingerprint (required)
OCI_FINGERPRINT=your_api_key_fingerprint_here

# OCI Tenancy OCID (required)
OCI_TENANCY_OCID=your_tenancy_ocid_here

# OCI Region (optional, defaults to us-chicago-1)
OCI_REGION=us-chicago-1

# OCI Private Key File Path (required)
# Must be an absolute path to your OCI API private key file
# Example: /Users/yourname/.oci/oci_api_key.pem
OCI_KEY_FILE=/path/to/your/oci_api_key.pem

# OCI Compartment OCID (required)
OCI_COMPARTMENT_ID=your_compartment_ocid_here

# Database URL (optional, defaults to SQLite)
DATABASE_URL=sqlite:///./llm_comparison.db

# LiteLLM Proxy Port (optional, defaults to 4000)
# The LiteLLM proxy runs on this port, FastAPI runs on port 8000
LITELLM_PORT=4000
